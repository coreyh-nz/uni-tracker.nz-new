spring:
  application:
    name: auth-service

jwt:
  access-lifetime: 15m
  refresh-lifetime: 30d
  private-key: "${JWT_PRIVATE_KEY}"
  public-key: "${JWT_PUBLIC_KEY}"
  issuer: "${JWT_ISSUER}"
  audience: "${JWT_AUDIENCE}"
